{
  "name": "moonbeam-dictionary",
  "version": "0.1.0",
  "description": "subquery diction for moonbase-alpha and moonriver",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "subql codegen"
  },
  "homepage": "https://github.com/subquery/moonbeam-dictionary",
  "repository": "github:subquery/moonbeam-dictionary",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Ian He",
  "license": "Apache-2.0",
  "resolutions": {
    "ipfs-unixfs": "6.0.6"
  },
  "dependencies": {
    "moonbeam-types-bundle": "^2.0.3"
  },
  "devDependencies": {
    "@polkadot/api": "^8.13.1",
    "@subql/cli": "1.3.1",
    "@subql/contract-processors": "latest",
    "@subql/types": "latest",
    "typescript": "^4.6.2"
  }
}
